#include<stdio.h>

float suma 
(float, float);
float resta (float, float);

int main(){
  int i, x4=0;
  float resultado_suma;
  char caracter = 'a';
  char cadena = "abc";
  int 4xx;

  for (i=0; i<20; i++){
     x4+=
     i;
     
     printf("Valor de %x4", x4);
  }
  
  resultado_suma = suma(+4.5, -8.9);
  resultado_resta = suma(-.4, +12.);
  @resultad+=^;

  if (resultado_suma > resultado_resta)
     printf("La suma es mayor");
  else
     printf("La resta es mayor");

  return(1);

}

float suma (float a, float b){
  return(a+b);
}

float resta (float a, float b){
  a++;
  b--;
  return(a - b);
}
