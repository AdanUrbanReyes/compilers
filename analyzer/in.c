#include<stdio.h>
#define PI 3.1416

float suma 
(float, float);
float resta (float, float);

int main(){
  long int i, x4=0;
  float resultado_suma;
  char caracter = 'a';
  char cadena = "abc";
  short int 4xx;

  for (i=0; i<20; i++){
     x4+=
     i;
     
     printf("Valor de %x4", x4);
  }

  while (i==67.56){
     rt=gf4+++.45  
	  
  }
  
  resultado_suma = suma(+4.5, -8.9);
  resultado_resta = suma(-.4, +12.);
  @resultad+=^;

  if (resultado_suma > resultado_resta && !rt)
     printf("La suma es mayor");
  else
     printf("La resta es mayor");

  return(1);
$
}

float suma (float a, float b){
  do{
     fg=er---4;
  }while(fr!=56);

  return(a+b);
}

float resta (float a, float b){

  switch (er){
     case 4:
	     ed = .25;
	  case 6:
	     ed = 25.;
  }
  a++;
  b--;
  return(a - b);
}
